{
  "name": "@kicad-part-finder/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No unit tests (deploy-only Worker)'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
